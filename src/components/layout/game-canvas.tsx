import styled from 'styled-components'

export const GameCanvas = styled.div`
  height: var(--vh);
  width: var(--vw);
  background: #0a0b0d;
  position: relative;
  overflow: hidden;
  color: white;
  padding: 1rem;
`

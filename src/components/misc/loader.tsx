import React from 'react'
import styled from 'styled-components'

const Wrapper = styled.div`
  background: white;
  height: var(--vh);
  width: var(--vw);
`

export const Loader = () => {
  return <Wrapper />
}
